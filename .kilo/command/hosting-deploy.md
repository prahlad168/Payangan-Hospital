# hosting-deploy

Invoke the `hosting-deploy` skill defined in `.agents/skills/hosting-deploy/SKILL.md`.

Use this command to deploy the Payangan Hospital website from the local Git repository to Idwebhost hosting.

See the skill file for the full workflow, configuration, and safety rules.
